/**
 * @typedef  {object} TestConfig
 * @property {string} name
 * @property {number} gold
 *//**
 * @param    {TestConfig} config 
 */
function test(config) {

}

test({})
/**
 * post_function 
 * @param {Object} params - an object.
 * @param {string} params.url - a url.
 * @param {Object} params.data - an data
 * @param {Function} params.onSuccess success function.
 * @param {Function} params.onFailure failure function.
 */
function _post(params){}
_post({url, da})

/** @description Determines the area of a circle that has the specified radius parameter.
 * @param {number} radius The radius of the circle.
 * @param {number} test Test the comment 
 * @return {number}
 */
function getArea(radius, test) {
    var areaVal;
    areaVal = Math.PI * radius * radius;
    return areaVal;
}


let obj = {
    /**
     * Test Object Method
     * @param {Object} input 
     * @param {string} input.name  - tree name
     * @param {number} input.index = tree index
     */
    test: function(input)
    {
        let { name , index } = input
        i
    }
}

obj.test({name:'this', index:})