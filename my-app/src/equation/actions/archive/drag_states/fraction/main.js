import { b_in_fraction } from './bottom.js'

import { t_in_fraction } from './top.js'

export let fraction_bottom = {
    in_fraction: b_in_fraction
}


export let fraction_top = {
    in_fraction: t_in_fraction
}
